import Phaser from "phaser";

class OptionsScene extends Phaser.Scene {
  constructor() {
    super({
      key: "OptionsScene",
      active: true,
    });
  }

  preload() {}
  create() {}
}

export default OptionsScene;
